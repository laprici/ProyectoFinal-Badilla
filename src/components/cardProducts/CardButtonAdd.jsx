import React from "react";

const CardButtonAdd = (props) => {
    return (
        <div>
            <button id="btn-add" className="btn btn-primary">Agregar</button>
        </div>
    )
}

export default CardButtonAdd;